import React, { Component } from 'react';
import {
  ColumnSearchPanel,
} from './style';

class SearchBox extends Component {
  render() {
    return (
       <ColumnSearchPanel className='show'>
        </ColumnSearchPanel>
    );
  }
}

export default SearchBox;